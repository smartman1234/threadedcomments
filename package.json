{
  "name": "threaded-comments",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "deploy": "npm run build --prefix ./static/comment && forge deploy && forge install --upgrade --confirm-scopes"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "eslint-plugin-react-hooks": "^2.1.2"
  },
  "dependencies": {
    "@forge/api": "^0.5.5",
    "@forge/resolver": "0.1.0-next.2"
  }
}
